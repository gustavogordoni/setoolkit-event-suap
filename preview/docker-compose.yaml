
services:
  php:
    build:
      context: ./php
      dockerfile: Dockerfile
    container_name: preview-json
    restart: always
    volumes:
      - "./:/var/www/html"
    
    ports:      
      - "8080:80"